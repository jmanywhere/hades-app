import type { Metadata } from "next";
import Image from "next/image";

export const metadata: Metadata = {
  title: "Hades Cash Home",
  description: "An improved ROI dapp",
};

export default function Home() {
  return (
    <main className="flex min-h-[calc(100vh-112px-64px)] flex-col items-center justify-between p-24">
      HOMEPAGE
    </main>
  );
}
